import React, { useContext } from 'react';
import { inVoicecontext } from '../Context/InvoiceContext';

const TimeLines = () => {
    const korim = useContext(inVoicecontext)
    return (
        <div>
            
        </div>
    );
}

export default TimeLines;
